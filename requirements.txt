# SampleLab — dipendenze (versioni allineate a quelle installate sul Mac di origine)
Flask==3.1.3
flask-cors==6.0.5
waitress==3.0.2
yt-dlp==2026.8.19
beautifulsoup4==4.15.0
mutagen==1.48.1
requests==2.34.2
pydub==0.25.1
soundfile==0.14.0
undetected-chromedriver==3.5.5
python-Levenshtein==0.27.4
# Opzionali (analisi audio / BPM): servono librerie pesanti
librosa==0.11.0
numpy==2.4.6
# Opzionale (conferma dal PARLATO nella pagina /verifica): trascrizione Whisper.
# Senza, la Verifica funziona lo stesso e la conferma dal parlato resta "non
# verificabile". Pesa ~1,5 GB fra dipendenze (ctranslate2, onnxruntime…) e modello
# (145-480 MB, scaricato la prima volta in `modelli/`, non versionata).
faster-whisper==1.2.1

# 🔗 Modifica dei video YouTube dall'app (19/09/2026): API ufficiale YouTube Data v3
# col consenso OAuth. Servono a «🔗 Collega YouTube» e ai pulsanti «✍️ Modifica sul
# video» (titolo, descrizione, tag). Richiedono un «client OAuth» creato una volta in
# Google Cloud (vedi il pannello nel tab 🗄️ Database). Senza queste librerie l'app
# funziona come prima e il pannello dice cosa installare.
google-api-python-client==2.200.0
google-auth-oauthlib==1.4.1

