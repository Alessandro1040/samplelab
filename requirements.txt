# SampleLab — dipendenze (versioni allineate a quelle installate sul Mac di origine)
Flask==3.1.3
flask-cors==6.0.5
waitress==3.0.2
yt-dlp==2026.8.19
beautifulsoup4==4.15.0
mutagen==1.48.1
requests==2.34.2
pydub==0.25.1
soundfile==0.14.0
undetected-chromedriver==3.5.5
python-Levenshtein==0.27.4
# Opzionali (analisi audio / BPM): servono librerie pesanti
librosa==0.11.0
numpy==2.4.6
# Opzionale (conferma dal PARLATO nella pagina /verifica): trascrizione Whisper.
# Senza, la Verifica funziona lo stesso e la conferma dal parlato resta "non
# verificabile". Pesa ~1,5 GB fra dipendenze (ctranslate2, onnxruntime…) e modello
# (145-480 MB, scaricato la prima volta in `modelli/`, non versionata).
faster-whisper==1.2.1
